import { ProfileDataTitleComponent } from '@components/profileDataTitle/profileDataTitle.styled';

export default function ProfileDataTitle({ children }) {
  return <ProfileDataTitleComponent>{children}</ProfileDataTitleComponent>;
}
