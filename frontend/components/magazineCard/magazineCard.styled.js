import styled from '@emotion/styled';

export let MagazineCardWrapper = styled.div`
  box-shadow: -10px 10px 40px -10px rgba(7, 30, 44, 0.17);
  border-radius: 10px;
  overflow: hidden;
`;

export let ImageWrapper = styled.div``;

export let Bottom = styled.div`
  padding: 30px;
`;

export let Text = styled.div``;

export let Date = styled.div`
  font-weight: 300;
  font-size: 14px;
  margin-bottom: 10px;
`;

export let TitleText = styled.p`
  font-weight: bold;
  font-size: 18px;
  margin-bottom: 25px;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
`;

export let Actions = styled.div`
  text-align: right;
`;
