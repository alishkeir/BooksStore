import styled from '@emotion/styled';
import colors from '@vars/colors';

export let DownloadLinkWrapper = styled.div`
  font-weight: normal;
  font-size: 14px;
  line-height: 16px;
  color: ${colors.monza};
  text-decoration: underline;
  cursor: pointer;
  display: inline;
`;
