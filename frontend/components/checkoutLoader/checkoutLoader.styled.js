import styled from '@emotion/styled';
import colors from '@vars/colors';

export let CheckoutLoaderComponent = styled.div`
  text-align: center;
`;

export let LoaderWrapper = styled.div`
  margin-bottom: 10px;
`;

export let LoaderIcon = styled.div``;

export let Text = styled.div`
  color: ${colors.monza};
`;
