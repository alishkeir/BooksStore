import styled from '@emotion/styled';
import breakpoints from '@vars/breakpoints';

export let SideModalRegistrationWrapper = styled.div`
  padding-top: 10px;

  @media (max-width: ${breakpoints.max.md}) {
    padding-top: 15px;
  }
`;

export let Form = styled.div``;

export let InputEmailWrapper = styled.div`
  margin-bottom: 25px;
`;

export let InputEmailAgainWrapper = styled.div`
  margin-bottom: 25px;
`;

export let InputPasswordWrapper = styled.div`
  margin-bottom: 25px;
`;

export let InputPasswordAgainWrapper = styled.div`
  margin-bottom: 25px;
`;

export let ButtonWrapper = styled.div`
  margin-bottom: 20px;
`;

export let AgreeMarketingWrapper = styled.div`
  margin-bottom: 25px;
`;

export let AgreeTacWrapper = styled.div`
  margin-bottom: 25px;
`;
